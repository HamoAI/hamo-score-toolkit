# One-command reference deployment:  docker compose up
# Services: ollama (model runtime) + setup (one-shot: fetch GGUF from HF, create model) + api (FastAPI /score)
services:
  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama-data:/root/.ollama
      - model-cache:/models
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 5s
      timeout: 3s
      retries: 20

  setup:
    image: curlimages/curl:latest
    depends_on:
      ollama:
        condition: service_healthy
    volumes:
      - model-cache:/models
    entrypoint: /bin/sh
    command:
      - -c
      - |
        set -e
        if [ ! -f /models/hamo-score-0.6b-v61.q8.gguf ]; then
          echo "downloading GGUF (639MB, one-time)..."
          curl -L --fail -o /models/hamo-score-0.6b-v61.q8.gguf \
            https://huggingface.co/HamoAI/hamo-score-0.6b/resolve/main/gguf/hamo-score-0.6b-v61.q8.gguf
        fi
        echo "creating model in ollama..."
        curl -s http://ollama:11434/api/create -d '{
          "model": "hamo-score-0.6b",
          "files": {"hamo-score-0.6b-v61.q8.gguf": "/models/hamo-score-0.6b-v61.q8.gguf"},
          "template": "<|im_start|>user\n{{ .Prompt }}<|im_end|>\n<|im_start|>assistant\n<think>\n\n</think>\n\n",
          "parameters": {"temperature": 0, "num_predict": 80, "stop": ["<|im_end|>"], "repeat_penalty": 1.0, "top_k": 0, "top_p": 1.0}
        }'
        echo "warmup..."
        curl -s http://ollama:11434/api/generate -d '{"model":"hamo-score-0.6b","prompt":"给来访者最新消息打分（AWEHB，0.0-3.0）。\n最新消息: 预热","stream":false,"keep_alive":-1}' > /dev/null
        echo "setup done"

  api:
    build:
      context: ..
      dockerfile: server/Dockerfile
    depends_on:
      setup:
        condition: service_completed_successfully
    environment:
      OLLAMA_URL: http://ollama:11434
      SCORE_MODEL: hamo-score-0.6b
    ports:
      - "8080:8080"

volumes:
  ollama-data:
  model-cache:
